{% extends 'LADRSecurityBundle:Security:layout.html.twig' %}

{% block content %}
    {{ form_start(form) }}
        {{ form_rest(form) }}
        <div class="form-group">
            <input type="submit" class="btn btn-block btn-primary" name="login" value="{{ "Inscription"|trans }}"/>
        </div>
    {{ form_end(form) }}
{% endblock %}
